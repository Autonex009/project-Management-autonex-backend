fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
jinja2>=3.1.0
pydantic>=2.0.0
pydantic[email]
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart>=0.0.6
openpyxl>=3.1.2
alembic>=1.11.0
apscheduler>=3.10.0,<4.0
# Embeddings for the policy search index (knowledge_service). 
# Using openai client since deepseek provides an openai compatible api.
# DeepSeek speaks the OpenAI wire format; this SDK is the client for it.
openai>=1.30.0
sse-starlette>=2.0.0
numpy>=1.26.0
encord>=0.1.150
arq>=0.25.0
redis>=5.0.0
reportlab>=4.0.0
docxtpl>=0.20.0   # Word template rendering (.docx → filled .docx)
xhtml2pdf>=0.2.11  # HTML → PDF fallback (used when LibreOffice is not available)
num2words==0.5.14

tenacity==8.3.0
